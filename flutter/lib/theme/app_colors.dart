// lib/theme/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const fire = Color(0xFFFF4D1C);
  static const fireSoft = Color(0xFFFF8C42);

  // Backgrounds
  static const bg = Color(0xFF08080F);
  static const bg2 = Color(0xFF0F0F1A);
  static const bg3 = Color(0xFF161625);
  static const surface = Color(0xFF1A1A2E);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textMuted = Color(0xFF888899);

  // Border
  static const border = Color(0x12FFFFFF);

  // Mode accents
  static const modeIndovina = Color(0xFFFF4D1C);
  static const modeBomb = Color(0xFFFF1C4D);
  static const modeImpostor = Color(0xFF7C1CFF);
  static const modeSpiega = Color(0xFF1CBBFF);
}
