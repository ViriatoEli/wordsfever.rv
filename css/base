/* ═══════════════════════════════════════
   WORDS FEVER · base.css
   Reset · Variables · Layout · Typography
═══════════════════════════════════════ */

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; -webkit-font-smoothing: antialiased; }

:root {
  --fire:    #FF4D1C;
  --fire2:   #FF8C42;
  --gold:    #FFDB58;
  --bg:      #07070E;
  --bg2:     #0D0D1A;
  --bg3:     #131323;
  --surface: #1A1A2E;
  --text:    #FFFFFF;
  --text2:   #B8B8CC;
  --muted:   #5E5E78;
  --border:  rgba(255,255,255,0.06);
  --c1: #FF4D1C; --c2: #FF1C4D; --c3: #7C1CFF; --c4: #1CBBFF;
  --fd: 'Bebas Neue', sans-serif;
  --fb: 'Nunito', sans-serif;
  --nav-h: 64px;
}

body {
  font-family: var(--fb);
  background: var(--bg);
  color: var(--text);
  overflow-x: hidden;
  animation: pageIn 0.45s ease both;
}
body.exit { animation: pageOut 0.35s ease both; }

/* noise overlay */
body::after {
  content: ''; position: fixed; inset: 0; pointer-events: none; z-index: 9999;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 512 512' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.75' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.025'/%3E%3C/svg%3E");
  opacity: 0.5;
}

::-webkit-scrollbar { width: 5px; }
::-webkit-scrollbar-track { background: var(--bg); }
::-webkit-scrollbar-thumb { background: var(--fire); border-radius: 3px; }
::selection { background: rgba(255,77,28,0.28); }

a   { color: inherit; text-decoration: none; }
img { display: block; max-width: 100%; }
button { cursor: pointer; font-family: var(--fb); border: none; background: none; }
li  { list-style: none; }

/* ── layout ── */
.container { max-width: 1140px; margin: 0 auto; padding: 0 2rem; }
.section    { max-width: 1140px; margin: 0 auto; padding: 6rem 2rem; }
.divider    { height: 1px; background: linear-gradient(90deg, transparent, var(--border) 40%, var(--border) 60%, transparent); }
.bg2-wrap   { background: var(--bg2); border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); }

/* ── text utils ── */
.grad {
  background: linear-gradient(135deg, var(--fire), var(--fire2) 55%, var(--gold));
  -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
}
.muted  { color: var(--muted); }
.text2  { color: var(--text2); }
.center { text-align: center; }

/* ── eyebrow ── */
.eyebrow {
  display: inline-flex; align-items: center; gap: 10px;
  font-size: 0.67rem; font-weight: 700; letter-spacing: 0.2em;
  text-transform: uppercase; color: var(--fire); margin-bottom: 0.75rem;
}
.eyebrow::before { content: ''; width: 18px; height: 2px; background: var(--fire); border-radius: 1px; flex-shrink: 0; }
.eyebrow.c { display: flex; justify-content: center; }
.eyebrow.c::before, .eyebrow.c::after { content: ''; width: 18px; height: 2px; background: var(--fire); border-radius: 1px; flex-shrink: 0; }

/* ── section titles ── */
.sec-title {
  font-family: var(--fd);
  font-size: clamp(2.4rem, 5vw, 4.2rem);
  letter-spacing: 0.03em; line-height: 0.94; margin-bottom: 0.75rem;
}
.sec-sub  { font-size: 0.96rem; color: var(--text2); max-width: 440px; line-height: 1.8; }
.sec-head { display: flex; justify-content: space-between; align-items: flex-end; flex-wrap: wrap; gap: 1.5rem; margin-bottom: 3rem; }
.sec-head.c { justify-content: center; text-align: center; flex-direction: column; align-items: center; }
.sec-head.c .sec-sub { max-width: 520px; }
